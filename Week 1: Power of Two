class Solution(object):
    def isPowerOfTwo(self, n):
        """
        :type n: int
        :rtype: bool
        """
        import math

        if n<=0:
            return False
        else:
            return math.floor(math.log10(n)/math.log10(2)) == math.ceil(math.log10(n)/math.log10(2))
